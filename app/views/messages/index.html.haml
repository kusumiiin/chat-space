.content
  = render "/templates/content-left"
  .content__right
    .content__right__top
      .content__right__top--group= @group.name
      .edit
        = link_to "Edit", edit_group_path(@group)
      %ul.members Members
      - @group.users.each do |user|
        %li.members__name= user.name
    .content__right__middle#message_wrapper
      #list
        = render @messages
    .content__right__bottom
      =form_for [@group, @message] do |f|
        =f.text_field :body, placeholder: "type a message", class: "type", id: "text_field"
        .content__right__bottom--icon
          = fa_icon "picture-o"
        =f.file_field :image, class: "file"
        .content__right__bottom--submit
          =f.submit "Send", class: "input", id: "input"
